added "vi-mode" patch
    modified to match US dvorak layout
        l -> s
        j -> n
        k -> t
    default paste from Clipboard, use Ctrl to paste from Primary
    modified ctrl bindings
       <C-h> = backspace
       <C-s> = delete
       <C-b> = normalmode
    Other ctrl bindings have been removed
